package Fundamentos;

public class CondicionalWhile {
    public static void main(String[] args) {

        // iniciando o contador
        int contador = 0;
        while(contador <= 10) {
            System.out.println(contador);
            // ou contador ++ ou ++ contador
            contador += 1; // contador iterando.
        }
    }
}
